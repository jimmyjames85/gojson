package gojson

import "testing"

type test struct {
	name string
}

func TestParseDigits(t *testing.T) {
	// tests := []test{}
	// for _, tt := range tests {
	// 	t.Run(tt.name, func(t *testing.T){
	// 		actual, c, err := ParseDigits()

	// 	})
	// }
}
